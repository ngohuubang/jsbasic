
const name = "Ngô Hữu Bằng"

const str1 = `My name is ${name} and I am FE
adbqwub
`;

console.log(str1)

//length : lấy độ dài chuỗi

const myStr = "FrontEnd Developer"

//cài extension visual studio inteligentcode

//hàm slpit : cắt chuỗi thành mảng theo ký tự trong hàm split
//toLowercase() : chuyển chuỗi thành chữ thường
//toUppercase() : chuyển thành chữ hoa
//.startsWith("FrontEnd"): kiểm tra chuỗi có bắt đầu với chữ FrontEnd không -> trả vê true flase
//.endsWith():giống startsWith những kiểm tra chữ cuối
//.includes(): kiểm tra chữ có chứa trong chuối hay không
//.indexOf(): trả về chữ đó nằm ở vị trí bao nhiêu
//.lastIndexOf(): kiểm tra cuối cùng nằm ở vị trí nào
//.replace("chữ cần thay","chữ thay")  : thay thế chuỗi
//.repeate(5) : lặp lại 5 lần
//.slice(start,length) : lấy từ vị trí nào, độ dài bao nhiêu
//.trim() : loại bỏ khoảng trống
//.charAt(0): lấy ra ký tự theo vị trí Index (ex : vị trí 0)
//.substr(index,length) : lấy ra 1 phần của chuỗi
//.substring(start index, end index):lấy ra kí của chuỗi